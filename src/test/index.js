'use strict'

const test = {
  endpoints: [
    {
      name: 'users',
      methods: [
        {
          method: 'GET',
          expected: 200,
          headers: [],
          body: []
        }
      ]
    }
  ]
}

export default test
